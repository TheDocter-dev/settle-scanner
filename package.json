{
  "name": "settle-scanner",
  "version": "0.1.0",
  "description": "Security testing harness for x402 merchant endpoints — passive by default, active probes only after domain-ownership verification.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "scan": "tsx src/cli.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
